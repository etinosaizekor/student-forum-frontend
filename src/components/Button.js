import Button from "react-bootstrap/esm/Button";
import styled from "styled-components";

const PageButton = styled(Button)`
    
    :hover{
        background-color: rgb(169, 201, 224)
    }
`

export default PageButton;